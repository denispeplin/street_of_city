<%= show_for @city do |s| %>
  <%= s.attribute :name %>
<% end %>

<%= link_to 'Edit', edit_city_path(@city) %> |
<%= link_to 'Back', cities_path %>

<br><br>

<%= link_to 'Districts', city_districts_path(@city) %> |
<%= link_to 'New District', new_city_district_path(@city) %>
